import { useState } from 'react'; import { Link, useNavigate } from 'react-router-dom'; import { PageContainer } from '../../components/layout/PageContainer'; import { Button } from '../../components/ui/Button'; import { createScript } from '../../utils/scriptsDb'
export function NewScriptPage() { const navigate = useNavigate(); const [title, setTitle] = useState(''); const [content, setContent] = useState(''); const [error, setError] = useState(''); const save = async () => { try { const script = await createScript(title, content); navigate(`/scripts/${script.id}`) } catch { setError('Unable to save script locally.') } }; return <PageContainer><div className="page-heading"><div><p className="eyebrow">Script editor</p><h1>New Script</h1></div><Link to="/scripts"><Button>Back</Button></Link></div>{error && <p className="storage-error">{error}</p>}<div className="script-form"><label>Title<input value={title} onChange={(event) => setTitle(event.target.value)} placeholder="Untitled Script" /></label><label>Script<textarea className="page-editor" value={content} onChange={(event) => setContent(event.target.value)} placeholder="Write or paste your script here..." /></label><Button variant="primary" onClick={() => void save()}>Save Script</Button></div></PageContainer> }
